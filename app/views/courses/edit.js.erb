$('#modal-window .modal-content').html('<%= escape_javascript(render partial: 'edit') %>');
$('#modal-window').modal('show')

$('#modal-window').on('shown.bs.modal', function() {
  $('#course_deadline').datepicker({
    format: "dd/mm/yy"
  });
});
